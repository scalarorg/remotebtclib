# remotebtclib

Push vault transaction:

```typescript
const staker = new Staker(
  stakerAddress,
  stakerPublicKey,
  servicePublicKey,
  covenantPublicKeys,
  quorum,
  tag,
  version,
  chainID,
  chainIdUserAddress,
  chainSmartContractAddress,
  mintingAmount
);
```
